"use strict";(globalThis.webpackChunkspec_binder_website=globalThis.webpackChunkspec_binder_website||[]).push([[8566],{7875(e,t,n){n.r(t),n.d(t,{default:()=>Q});var o=n(1656),a=n(6540),r=n(4164),s=n(7559),i=n(3104),d=n(6347),l=n(205),c=n(7485),h=n(1682),p=n(679);function u(e){return a.Children.toArray(e).filter(e=>"\n"!==e).map(e=>{if(!e||(0,a.isValidElement)(e)&&function(e){const{props:t}=e;return!!t&&"object"==typeof t&&"value"in t}(e))return e;throw new Error(`Docusaurus error: Bad <Tabs> child <${"string"==typeof e.type?e.type:e.type.name}>: all children of the <Tabs> component should be <TabItem>, and every <TabItem> should have a unique "value" prop.`)})?.filter(Boolean)??[]}function g(e){const{values:t,children:n}=e;return(0,a.useMemo)(()=>{const e=t??function(e){return u(e).map(({props:{value:e,label:t,attributes:n,default:o}})=>({value:e,label:t,attributes:n,default:o}))}(n);return function(e){const t=(0,h.XI)(e,(e,t)=>e.value===t.value);if(t.length>0)throw new Error(`Docusaurus error: Duplicate values "${t.map(e=>e.value).join(", ")}" found in <Tabs>. Every value needs to be unique.`)}(e),e},[t,n])}function m({value:e,tabValues:t}){return t.some(t=>t.value===e)}function w({queryString:e=!1,groupId:t}){const n=(0,d.W6)(),o=function({queryString:e=!1,groupId:t}){if("string"==typeof e)return e;if(!1===e)return null;if(!0===e&&!t)throw new Error('Docusaurus error: The <Tabs> component groupId prop is required if queryString=true, because this value is used as the search param name. You can also provide an explicit value such as queryString="my-search-param".');return t??null}({queryString:e,groupId:t});return[(0,c.aZ)(o),(0,a.useCallback)(e=>{if(!o)return;const t=new URLSearchParams(n.location.search);t.set(o,e),n.replace({...n.location,search:t.toString()})},[o,n])]}function f(e){const{defaultValue:t,queryString:n=!1,groupId:o}=e,r=g(e),[s,i]=(0,a.useState)(()=>function({defaultValue:e,tabValues:t}){if(0===t.length)throw new Error("Docusaurus error: the <Tabs> component requires at least one <TabItem> children component");if(e){if(!m({value:e,tabValues:t}))throw new Error(`Docusaurus error: The <Tabs> has a defaultValue "${e}" but none of its children has the corresponding value. Available values are: ${t.map(e=>e.value).join(", ")}. If you intend to show no default tab, use defaultValue={null} instead.`);return e}const n=t.find(e=>e.default)??t[0];if(!n)throw new Error("Unexpected error: 0 tabValues");return n.value}({defaultValue:t,tabValues:r})),[d,c]=w({queryString:n,groupId:o}),[h,u]=function({groupId:e}){const t=function(e){return e?`docusaurus.tab.${e}`:null}(e),[n,o]=(0,p.Dv)(t);return[n,(0,a.useCallback)(e=>{t&&o.set(e)},[t,o])]}({groupId:o}),f=(()=>{const e=d??h;return m({value:e,tabValues:r})?e:null})();(0,l.A)(()=>{f&&i(f)},[f]);return{selectedValue:s,selectValue:(0,a.useCallback)(e=>{if(!m({value:e,tabValues:r}))throw new Error(`Can't select invalid tab value=${e}`);i(e),c(e),u(e)},[c,u,r]),tabValues:r}}var b=n(2303);const v="tabList__CuJ",y="tabItem_LNqP";var k=n(4848);function x({className:e,block:t,selectedValue:n,selectValue:o,tabValues:a}){const s=[],{blockElementScrollPositionUntilNextRender:d}=(0,i.a_)(),l=e=>{const t=e.currentTarget,r=s.indexOf(t),i=a[r].value;i!==n&&(d(t),o(i))},c=e=>{let t=null;switch(e.key){case"Enter":l(e);break;case"ArrowRight":{const n=s.indexOf(e.currentTarget)+1;t=s[n]??s[0];break}case"ArrowLeft":{const n=s.indexOf(e.currentTarget)-1;t=s[n]??s[s.length-1];break}}t?.focus()};return(0,k.jsx)("ul",{role:"tablist","aria-orientation":"horizontal",className:(0,r.A)("tabs",{"tabs--block":t},e),children:a.map(({value:e,label:t,attributes:o})=>(0,k.jsx)("li",{role:"tab",tabIndex:n===e?0:-1,"aria-selected":n===e,ref:e=>{s.push(e)},onKeyDown:c,onClick:l,...o,className:(0,r.A)("tabs__item",y,o?.className,{"tabs__item--active":n===e}),children:t??e},e))})}function S({lazy:e,children:t,selectedValue:n}){const o=(Array.isArray(t)?t:[t]).filter(Boolean);if(e){const e=o.find(e=>e.props.value===n);return e?(0,a.cloneElement)(e,{className:(0,r.A)("margin-top--md",e.props.className)}):null}return(0,k.jsx)("div",{className:"margin-top--md",children:o.map((e,t)=>(0,a.cloneElement)(e,{key:t,hidden:e.props.value!==n}))})}function T(e){const t=f(e);return(0,k.jsxs)("div",{className:(0,r.A)(s.G.tabs.container,"tabs-container",v),children:[(0,k.jsx)(x,{...t,...e}),(0,k.jsx)(S,{...t,...e})]})}function E(e){const t=(0,b.A)();return(0,k.jsx)(T,{...e,children:u(e.children)},String(t))}const C="tabItem_Ymn6";function A({children:e,hidden:t,className:n}){return(0,k.jsx)("div",{role:"tabpanel",className:(0,r.A)(C,n),hidden:t,children:e})}const R=JSON.parse('{"Z":[{"key":"framework","label":"Framework","tagline":"Compile-time Gherkin to JUnit 5 code generator","versionUrlTemplate":"https://central.sonatype.com/artifact/dev.specbinder/annotation-processor/{version}","embedCardUrl":null,"versions":[{"version":"Unreleased","date":null,"unreleased":true,"sections":[{"type":"Changed","items":["Execution reporter: <code>executedAt</code> in the per-feature JSON report now names the most recent moment among the scenarios the report holds, rather than the oldest. Since a run\'s own results are always newer than anything carried over from an earlier one, the field now reads as when the report was last brought up to date \u2014 the natural thing to show against a spec \u2014 where previously re-running a single scenario left the header pinned to whichever scenario had gone longest without running. Consumers displaying this as a \\"last run\\" time no longer need to derive it from the scenarios themselves. An outline node\'s moment likewise comes from its most recent example row instead of its earliest"]}]},{"version":"2026.47.0","date":"2026-09-02","unreleased":false,"sections":[{"type":"Added","items":["Execution reporter: re-running only part of a spec \u2014 a single scenario, a single Rule, or one example row of a Scenario Outline \u2014 now folds that run\'s results into the per-feature JSON report already on disk instead of replacing the file. Previously any partial run left behind a report describing only what had just run, discarding every other scenario\'s outcome, so keeping a complete picture meant re-running the whole spec every time. A scenario that did not run keeps the outcome of its last execution, and the report\'s summary, start time and total duration are re-derived from everything the merged report holds. Results are matched to scenarios by position and by content together, because neither survives editing on its own: a scenario that moved keeps its outcome, one whose own steps changed keeps the hash recorded against it so a consumer can still tell the outcome describes an older version, and one deleted from the spec is dropped rather than lingering as a failure nothing can clear. Example rows are matched on their values rather than their place in the <code>Examples</code> table, so reordering the table does not shuffle outcomes between rows, and a row no longer in the table takes its result with it; the merged rows are laid out in the order the table now reads. Merging happens only when the file on disk fits the run \u2014 readable, describing this same feature, and carrying the current report schema version \u2014 so a stale or foreign report is replaced rather than allowed to contribute results. A full run needs no special handling and produces exactly what it did before"]},{"type":"Changed","items":["<code>@Gherkin2JUnitOptions(emitScenarioHash = ...)</code> now defaults to <code>true</code>, so every generated <code>@Test</code> and <code>@ParameterizedTest</code> method carries a <code>@ScenarioHash</code> annotation unless the option is explicitly set to <code>false</code>. The option had quietly grown into far more than its name suggests: with it switched off the execution report also loses the original Gherkin step text, a Scenario Outline\'s template steps and its typed step arguments, and any way to tell an outcome recorded against an older version of a scenario from one describing the spec as it stands today \u2014 so the previous default handed most projects a materially thinner report than they had any reason to expect. Existing projects need no source change and the generated tests behave exactly as before; only something pinning the exact text of a generated class or the exact JSON of an execution report will notice the added annotation and the richer report. Set <code>emitScenarioHash = false</code> on the marker class to keep the previous output. The processor\'s verbose startup log reports the new default accordingly"]}]},{"version":"2026.46.0","date":"2026-08-29","unreleased":false,"sections":[{"type":"Added","items":["<strong>(experimental)</strong> New opt-in <code>@Gherkin2JUnitOptions(stripBetweenPatterns = {...})</code> option that declares the two ends of a span of spec file text as separate patterns \u2014 <code>@StripBetween(start = \\"...\\", end = \\"...\\")</code> \u2014 and strips everything from the start marker to the end marker, markers included. It is the safer companion to <code>stripPatterns</code> for deleting text retired by an issue, such as <code>&lt;REMOVED BR-789&gt;legacy discount &lt;/REMOVED BR-789&gt;</code>. Expressing the same span as a single regex has three traps that each produce a successful build with quietly wrong output: forgetting the dot-all flag makes a span crossing a newline silently fail to match; a greedy quantifier makes the span run to the last closing marker in the file, deleting everything in between; and an unclosed marker simply does not match. Declaring the ends separately removes the first two entirely, because the span is located by position rather than by one regex \u2014 so neither pattern needs a flag to cross lines, and each start always pairs with the nearest following end. A span may wrap whole Gherkin constructs such as several steps, an entire Scenario, or table rows; nesting is not supported, and a start with no following end leaves the text untouched. These pairs are applied before <code>stripPatterns</code>, so a span still disappears wholesale even when a pattern there would also have matched its markers individually; the default is an empty list, which leaves the spec file untouched"]}]},{"version":"2026.45.0","date":"2026-08-24","unreleased":false,"sections":[{"type":"Added","items":["Execution reporter: a Scenario Outline in the per-feature JSON report now carries a <code>templateSteps</code> array on the outline node \u2014 a single view of the outline\'s own steps with their <code>&lt;&gt;</code> placeholders intact, each entry pairing the called <code>methodName</code> with the original Gherkin <code>text</code>. A template step whose spec has a trailing DocString or DataTable also carries it as a typed <code>arguments</code> entry in template form \u2014 placeholders left unresolved, and DataTables keyed by their column headers without the runtime <code>columns</code> mapping since no row object is bound to the template. This complements the per-row <code>examples</code>, which resolve those placeholders to concrete values, so consumers can render the outline template once instead of re-deriving it from every row. Gated on the same scenario-hash match as step <code>text</code>; omitted when the hash is absent or the spec has been edited since the test was generated","Execution reporter: a Scenario Outline in the per-feature JSON report now carries an aggregate <code>status</code> on the outline node, rolled up from its example rows worst-first \u2014 failed if any row failed, else aborted if any row aborted, else skipped if any row was skipped, else passed. Previously the outline node had no status of its own, so consumers had to derive one from the rows; an outline whose rows all passed could be shown as not-executed","<strong>(experimental)</strong> New opt-in <code>@Gherkin2JUnitOptions(stripPatterns = {...})</code> option that strips matching text out of a spec file before it is turned into test code. Teams that annotate specs with revision markers tying wording back to an issue tracker \u2014 for example <code>&lt;CHANGED BR-123&gt;premium&lt;/CHANGED BR-123&gt;</code> or <code>&lt;REMOVED BR-789&gt;legacy discount &lt;/REMOVED BR-789&gt;</code> \u2014 previously found those markers reaching the generated code: they became part of step method names, so adding or editing a marker renamed an abstract step method and broke the hand-written test class implementing it, and they corrupted field names derived from data table headers and emitted unbalanced HTML into JavaDoc. Every match of every configured regular expression is removed, so the shape of the pattern decides what disappears: match only a marker and the text it wrapped survives, match an opening marker through a closing marker and the wrapped text goes with it. Text is stripped everywhere it can appear \u2014 step text, Feature/Rule/Scenario names, descriptions, doc strings, data tables and <code>Examples</code> tables, including header cells. A pattern may wrap whole Gherkin constructs such as several steps, an entire Scenario, or table rows; any line left holding only whitespace is dropped, so removing a table row does not leave a gap that would break the table, though this shifts the source line numbers of everything below it. Patterns are applied in the order declared, which matters when they overlap \u2014 a marker-only pattern listed first can strip the markers a wrapping pattern was relying on, so list wrapping patterns first. A match that takes all of a step\u2019s text but leaves its keyword behind fails the build with a message naming the line; the default is an empty list, which leaves the spec file untouched"]},{"type":"Changed","items":["The <code>@BeforeEach</code> method generated from a feature-level <code>Background</code> is now named <code>background</code> (previously <code>featureBackground</code>). Rule-level background methods are unchanged (<code>ruleBackground</code>)","Internal terminology cleanup: the annotation processor\'s internal identifiers and comments that still used the old \\"inject\\" wording were renamed to \\"resolved\\" (e.g. the trailing parameters propagated from a matched base step method), completing the alignment behind the earlier <code>@JUnitInject</code> \u2192 <code>@JUnitResolved</code> rename. No behavior or public API change","<strong>\u26a0\ufe0f BREAKING:</strong> Scenario Outline example rows are now named <code>Example: [...]</code> instead of <code>Example 1: [...]</code> \u2014 the row number was dropped from the generated <code>@ParameterizedTest</code> display name, so IDE/CI test output and the <code>displayName</code> in execution reports no longer include it. Tooling that matches on the <code>Example N:</code> prefix must be updated; rows remain distinguishable by their <code>[argument = value, ...]</code> suffix"]},{"type":"Fixed","items":["Execution reporter: per-step outcomes are now captured for tests that use a per-class test-instance lifecycle (JUnit\'s <code>@TestInstance(Lifecycle.PER_CLASS)</code>, such as a base class with a non-static <code>@BeforeAll</code>). Previously every step in such a feature was reported as <code>skipped</code> in the JSON report even when it actually ran and passed or failed; now each step\'s true status, timing, and error are recorded just as they are under the default per-method lifecycle"]},{"type":"Removed","items":["<strong>\u26a0\ufe0f BREAKING:</strong> Execution reporter: the <code>sourceLine</code> field has been removed from every node of the per-feature JSON report \u2014 scenarios, Scenario Outlines, example rows and Rules. The field could only ever be populated when the generated test code carried a source-line marker, which generation does not produce, so in practice it was absent from real reports and present only in hand-written test classes. Scenarios and Rules are no longer re-sorted by source line either; they now appear in the order they executed, which for generated code is spec order. Tooling reading <code>sourceLine</code> should locate a scenario by its <code>scenarioHash</code> or its <code>id</code> instead"]}]},{"version":"2026.44.0","date":"2026-07-11","unreleased":false,"sections":[{"type":"Added","items":["The <code>@Gherkin2JUnit</code> path now accepts a leading <code>./</code> to anchor matching at the annotated class\'s own package directory instead of the classpath root \u2014 for both glob patterns (e.g. <code>\\"./**/*.feature\\"</code>) and individual files (e.g. <code>\\"./Cart.feature\\"</code>). This lets a marker class pick up the feature files sitting alongside it without repeating its full package path; generated class names and packages still mirror each matched file\'s real location. A <code>./</code> pattern that matches nothing reports the pattern exactly as written","New opt-in <code>@Gherkin2JUnitOptions(skipUnchangedSpecs = true)</code> flag that skips regenerating a test class when none of its generation inputs have changed since it was last generated \u2014 cutting wasted work on incremental builds for projects with many feature files. The generator stamps each generated class with a <code>@SourceTimestamp</code> recording the newest last-modified time across the spec file, the marker class, and the marker\'s class hierarchy (where options typically live, so editing options forces regeneration); on a later run it re-stamps only when that value has advanced. A previously generated class that is missing or carries no recorded timestamp \u2014 for example after a clean build \u2014 always regenerates, so the optimization never leaves stale output behind. Detection follows the newest input time, so a change that does not advance it (such as a <code>git checkout</code> of an older revision) is not picked up; the default remains <code>false</code>"]},{"type":"Changed","items":["<code>@Gherkin2JUnitOptions(skipGenerationForTags = ...)</code> now keeps tagged Scenarios and Rules visible in the generated test class as skipped tests instead of omitting them entirely: the <code>@Test</code> method is still generated with its Gherkin steps rendered as a comment, and its body reports the test as skipped via <code>Assumptions.assumeTrue(false, ...)</code>; a tagged Rule\'s nested class is generated with each of its scenarios skipped this way. A tagged Feature generates no test class at all \u2014 the whole feature is dropped from generation. This surfaces manual or work-in-progress items as skipped in test reports rather than silently disappearing from the suite","The <code>@ScenarioHash</code> value now ignores inter-word whitespace within a step, so steps that differ only in the number of spaces between words produce the same hash \u2014 matching the existing behavior where blank lines between steps and the step keyword (<code>Given</code>/<code>When</code>/<code>Then</code>/<code>And</code>/<code>But</code>/<code>*</code>) already have no effect. Downstream tooling comparing hashes against live source no longer sees spurious changes from cosmetic re-spacing"]},{"type":"Fixed","items":["Execution reporter: a hand-written <code>@BeforeEach</code> lifecycle method on a base or marker class (for example a <code>setUp()</code> that builds a test fixture) is no longer mistaken for a Gherkin <code>Background</code>. Only <code>@BeforeEach</code> methods on SpecBinder-generated test classes contribute background steps now, so infrastructure setup \u2014 and any non-serializable values it passes around \u2014 no longer leak into the per-feature JSON report as spurious background steps or step arguments","Execution reporter: the per-feature JSON report is now written atomically \u2014 serialized in full, then moved into place \u2014 so a failure partway through serialization can no longer leave a truncated, unparseable report on disk. A previously written valid report survives intact, and a consumer reading the file never observes a partially written report","Gherkin description text (under a Feature, Rule, Scenario, or Background) that contains a <code>*/</code> sequence \u2014 for example a glob pattern such as <code>**/*.feature</code> \u2014 no longer produces uncompilable generated test code. Such descriptions are rendered as a JavaDoc comment by default, and the sequence previously closed that comment early; it is now emitted so it stays inside the comment and reads as written"]}]},{"version":"2026.43.0","date":"2026-06-28","unreleased":false,"sections":[{"type":"Added","items":["Execution reporter: each step in the per-feature JSON report now carries a <code>publishedReporterEntries</code> array of <code>TestReporter.publishEntry(...)</code> calls made from that step\'s code, each with its key, value, and timestamp \u2014 so structured per-step diagnostics flow through to downstream tooling alongside status and timing","Execution reporter: each step in the per-feature JSON report now carries a <code>text</code> field with the original Gherkin line (keyword + spec text verbatim) for Backgrounds, scenarios, and Scenario Outline example rows \u2014 so consumers can render a readable Given/When/Then trace without re-parsing the <code>.feature</code> file; omitted when the spec has been edited since the test was generated","Execution reporter: each entry of a step\'s <code>arguments</code> array now self-describes its Gherkin kind via a <code>{type, value}</code> envelope \u2014 <code>type</code> is one of <code>simple</code>, <code>docString</code>, or <code>dataTable</code>. DocString entries additionally carry the opening-fence media-type identifier (e.g. <code>html</code> from <code>\\"\\"\\"html</code>) on a <code>mediaType</code> field. DataTable entries additionally carry a <code>columns</code> array of <code>{header, field}</code> pairs in source-file column order, so consumers know both the spec-verbatim column heading and the JSON key used in each row \u2014 useful when SpecBinder rewrites multi-word headers like <code>User Name</code> to <code>userName</code>. Gated on the same scenario-hash match as <code>text</code> stamping; on a mismatch or absent hash, <code>arguments</code> stay as today\'s bare runtime values","New opt-in <code>@Gherkin2JUnitOptions(descriptionAsAnnotation = true)</code> flag that emits Gherkin description text (under Feature / Rule / Scenario / Background) as a runtime-retained <code>@Description(\\"\\"\\"\u2026\\"\\"\\")</code> annotation on the corresponding generated class or method \u2014 placed immediately below <code>@DisplayName</code> \u2014 instead of the default JavaDoc block, so downstream tooling can read the description at runtime via reflection. The two emission modes are mutually exclusive; the default remains JavaDoc and existing projects see no change","Execution reporter: when <code>descriptionAsAnnotation</code> is enabled, the per-feature JSON report now carries the Gherkin description text on a new <code>description</code> field at the Feature, Rule, and Scenario level (including the Scenario Outline parent \u2014 example rows share the parent\'s description) \u2014 so consumers can render the full Gherkin context next to each heading without re-parsing the <code>.feature</code> file; omitted when the option is off or the element has no description","Execution reporter: steps marked <code>skipped</code> (because an earlier step in the same scenario failed) now also carry their <code>arguments</code> array in the per-feature JSON report, populated from the parsed <code>.feature</code> / <code>.specb</code> source instead of from a runtime call \u2014 so consumers can render the same DocString body, DataTable rows, and quoted inline values they would have seen had the step actually run. Excludes Scenario Outline example rows, whose spec text carries <code>&lt;placeholder&gt;</code> tokens rather than per-example values; gated on the same scenario-hash match as <code>text</code> stamping","DocStrings that would exceed the JVM\'s per-string-literal byte limit (65535 bytes for one constant-pool entry) are now automatically emitted as multiple plain string-literal chunks joined at runtime via <code>String.join(\\"\\", \u2026)</code>, so generated test classes compile even when an individual DocString \u2014 e.g. a base64-encoded fixture or snapshot image \u2014 is very large. The <code>+</code> operator can\'t serve this purpose because javac compile-time-folds adjacent string-literal <code>+</code> expressions back into a single constant-pool entry; <code>String.join</code> defers concatenation to runtime so each chunk stays its own constant. Below the configurable cap the existing single Java text-block emission is preserved unchanged. A new <code>@Gherkin2JUnitOptions(maxStringLiteralBytes = N)</code> option configures the cap (default 65000, just under the JVM hard limit); tests can lower it to exercise the chunking with small inputs"]},{"type":"Changed","items":["<strong>\u26a0\ufe0f BREAKING:</strong> The <code>@JUnitInject</code> marker annotation has been renamed to <code>@JUnitResolved</code>, aligning with JUnit\'s own \\"parameter resolution\\" terminology (its <code>ParameterResolver</code> extension point) rather than implying dependency injection. Its behavior is unchanged \u2014 mark a custom resolver-supplied step-method parameter, or its type, so the parameter is propagated to the generated test method. Update any <code>import dev.specbinder.annotations.JUnitInject;</code> and <code>@JUnitInject</code> usages to the new name","Class-level <code>@DisplayName</code> on the generated test class now reflects the Feature title line (e.g., <code>Feature: Shopping Cart</code>) instead of the annotated class name, matching the convention already used for <code>Rule:</code> and <code>Scenario:</code> <code>@DisplayName</code> values; when <code>addSourceLineNumbers</code> is enabled the line number is included as <code>Feature [N]: ...</code>. The class-level JavaDoc now contains only the feature description lines and is omitted entirely when the feature has no description, so reports and generated code read consistently with the Gherkin source","Execution reporter: the per-feature JSON report\'s top-level <code>displayName</code> now reflects the Gherkin Feature title regardless of whether SpecBinder runs in concrete or abstract generation mode \u2014 in abstract mode the reporter now walks the JUnit test class\'s superclass chain to read the generated class\'s <code>@DisplayName</code> rather than falling back to the user-written concrete subclass\'s simple name","Execution reporter: the per-feature JSON report\'s <code>generatedClass</code> field has been renamed to <code>testClass</code>, to accurately reflect that it names the test class that was actually run \u2014 which is not always the same as the generated class","Execution reporter: per-feature JSON report\'s <code>schemaVersion</code> bumped from 6 to 8 to reflect the new typed-arguments envelope shape and the renamed <code>testClass</code> field","<strong>\u26a0\ufe0f BREAKING:</strong> The default value of <code>@Gherkin2JUnitOptions(useCucumberAnnotationsForStepMatching = ...)</code> has been flipped from <code>true</code> to <code>false</code>. By default the generator now matches existing step implementations in the base/marker class by method name only, ignoring <code>@Given</code>/<code>@When</code>/<code>@Then</code> annotation values. Projects that relied on annotation-pattern matching \u2014 typically when migrating from Cucumber and keeping descriptive method names \u2014 must now opt back in with <code>@Gherkin2JUnitOptions(useCucumberAnnotationsForStepMatching = true)</code>, otherwise those steps will no longer be recognised as implemented and will be (re)generated"]},{"type":"Fixed","items":["Execution reporter: per-step <code>arguments</code> no longer accumulate nested <code>{type, value, \u2026}</code> wrappers when the feature has Gherkin Rules \u2014 previously each Rule\'s <code>@Nested</code> JUnit class triggered an extra finalisation pass that re-wrapped the already-wrapped arguments, producing JSON like <code>value: {type: docString, value: {type: docString, value: \u2026}}</code> with depth equal to (number of Rules + 1)"]}]},{"version":"2026.42.0","date":"2026-06-04","unreleased":false,"sections":[{"type":"Added","items":["Propagation of JUnit 5\'s built-in injected parameters (<code>TestInfo</code>, <code>TestReporter</code>, <code>@TempDir Path</code> / <code>@TempDir File</code>) from base step methods through to the generated <code>@Test</code> / <code>@BeforeEach</code> / <code>@ParameterizedTest</code> methods \u2014 declared on the step method signature in the base/marker class, they are auto-forwarded on the step call and aggregated (deduplicated by name) across all steps of the enclosing scenario or background, with <code>@TempDir</code> and any other parameter annotations preserved verbatim on the generated method parameter","New <code>@JUnitInject</code> marker annotation (<code>@Target({PARAMETER, TYPE})</code>) that extends the same propagation to custom user-defined types resolved by a JUnit <code>ParameterResolver</code> \u2014 placed on the parameter directly or once on the type\'s class declaration; works in concrete and abstract generation modes, in scenarios, scenario outlines, and backgrounds; all non-<code>@JUnitInject</code> annotations on the source parameter are preserved verbatim so the user\'s resolver can observe them at runtime"]}]},{"version":"2026.39.0","date":"2026-05-25","unreleased":false,"sections":[{"type":"Added","items":["Automatic wrapping of Gherkin values in a domain value object\'s static factory method when the target parameter or DataTable column maps to a non-enum custom type \u2014 covers quoted step parameters and <code>LIST_OF_OBJECT_PARAMS</code> cell values, including overload disambiguation by inferred value type with a unique-<code>String</code>-factory fallback"]},{"type":"Changed","items":["Default value of <code>@Gherkin2JUnitOptions.shouldBeAbstract</code> flipped from <code>false</code> to <code>true</code>, making abstract test class generation the new default \u2014 opt back into concrete generation with <code>@Gherkin2JUnitOptions(shouldBeAbstract = false)</code>"]}]},{"version":"2026.38.0","date":"2026-05-05","unreleased":false,"sections":[{"type":"Added","items":["Execution reporter: a JUnit 5 extension that captures hierarchical execution results (feature, rule, scenario, step) and writes per-feature JSON reports to <code>target/specbinder-reports/</code>","Step-level reporting via ByteBuddy bytecode instrumentation \u2014 intercepts step method calls at runtime to track individual step status, timing, and errors without requiring source-level changes","Bytecode call-site scanner that discovers ordered step method sequences from generated test class bytecode"]},{"type":"Changed","items":["Scenario test methods inside rule nested classes are now named <code>rule_N_scenario_M</code> instead of <code>scenario_M</code> to include the rule index prefix"]},{"type":"Fixed","items":["DataTable object type naming when step text ends with non-alphanumeric characters (e.g., <code>\\"Given the following reports :\\"</code> with a trailing space before the colon)"]}]},{"version":"2026.37.0","date":"2026-04-06","unreleased":false,"sections":[{"type":"Changed","items":["automated tests setup","Simplified GitHub Release creation step in the release workflow"]}]},{"version":"2026.33.0","date":"2026-04-06","unreleased":false,"sections":[{"type":"Changed","items":["Upgraded GitHub Actions to v5 to resolve Node.js 20 deprecation warnings"]},{"type":"Fixed","items":["Excluded auto-generated source code archives from GitHub Releases"]}]},{"version":"2026.32.0","date":"2026-04-06","unreleased":false,"sections":[{"type":"Changed","items":["Upgraded GitHub Actions to v5 to resolve Node.js 20 deprecation warnings"]},{"type":"Fixed","items":["Excluded auto-generated source code archives from GitHub Releases"]}]},{"version":"2026.31.0","date":"2026-04-06","unreleased":false,"sections":[{"type":"Fixed","items":["Fixed GitHub Release creation failing due to shell interpretation of backticks in changelog notes"]}]},{"version":"2026.30.0","date":"2026-04-06","unreleased":false,"sections":[{"type":"Added","items":["Support for escaping spaces and backslashes inside DocString step argument types"]},{"type":"Changed","items":["Adopted calendar year as the major version in the versioning scheme (e.g., <code>2026.30.0</code>)","Excluded source code archives from GitHub Releases"]}]},{"version":"0.29.0","date":"2026-04-06","unreleased":false,"sections":[{"type":"Fixed","items":["Fixed release profile running on child modules by adding <code>inherited=false</code>"]}]},{"version":"0.28.0","date":"2026-04-06","unreleased":false,"sections":[{"type":"Added","items":["Support for escaping spaces and backslashes in Gherkin elements","Automated release workflow via GitHub Actions triggered by <code>rc</code> tag push","Changelog stamping as part of the release process"]},{"type":"Changed","items":["Moved to automated versioning process with minor version increments"]}]}]},{"key":"plugin","label":"IDE Plugin","tagline":null,"versionUrlTemplate":null,"embedCardUrl":"https://plugins.jetbrains.com/embeddable/card/30540","versions":[{"version":"Unreleased","date":null,"unreleased":true,"sections":[{"type":"Fixed","items":["Re-running a single scenario no longer strips the status from every other scenario in the file. Now that a run adds its result to the execution report instead of replacing it, a report can cover only some of the scenarios \u2014 and the status of each one is found by the scenario it was recorded against rather than by counting down the report, so the scenarios that were not re-run keep the result of their own last run. Scenarios inside a Rule, and scenarios that have moved because another was added above them, are matched the same way.","A result recorded for one scenario could be shown against a different scenario that happened to have identical steps. The scenario that actually ran now gets its result, and the other reads as not executed.","After re-running a single scenario, clicking a status lozenge on the editor toolbar could jump to the wrong scenario. The counts themselves were right, but a result could be credited to the scenario above the one that produced it, so the caret landed there instead. Each scenario is now counted and navigated to from its own result.","In the split editor, clicking a scenario\'s row in the Execution Report pane could highlight and scroll to a different scenario after a single-scenario re-run. The report pane\'s rows are now linked to the scenario each result came from, so highlighting and scroll-sync follow the right one.","Undoing an edit brings a scenario\'s recorded status back. Changing a scenario\'s steps drops it to not executed, because the recorded result was for different wording \u2014 and putting the steps back as they were now restores the status it had, instead of leaving it not executed until the next run.","An Examples row edited since the run showed the plain run icon instead of the not-executed chevron every other element uses. A row edited since it ran now reads as not executed, the same as a row that never ran.","The time shown in the editor toolbar\'s report section no longer claims more than it can. A report can now hold results from several runs, so the tooltip says the report was last updated at that time rather than that it was generated then \u2014 the time itself is the most recent run in the file, and results carried over from earlier runs may be older.","An Examples row of a Scenario Outline could show the result of a neighbouring row. Each row is now matched to its result by its own values, so re-running one row leaves the others alone, and reordering the Examples table carries every row\'s status along with it.","A scenario written with an alternative keyword is no longer invisible to the features that pair it with its generated test. A scenario headed <code>Example:</code> or <code>Scenario Template:</code> was being looked for under the canonical <code>Scenario:</code> / <code>Scenario Outline:</code> wording, so nothing could be matched to it. Its gutter Run icon opened an empty \\"Nothing here\\" popup instead of offering Run and Debug; its recorded pass/fail status was blank in the gutter and on the toolbar; Find Usages on a step method left out the steps inside it; and Shift+Ctrl/Cmd+Click on a step method call would not open it. Rules and Features were unaffected, which is why only scenarios appeared to be dead. Gradle-managed projects were affected in the same way."]}]},{"version":"2026.17.0","date":"2026-08-29","unreleased":false,"sections":[{"type":"Added","items":["Revision markup in spec files is now highlighted in its own colour. Text that SpecBinder strips before generating tests \u2014 whatever the <code>stripPatterns</code> option matches \u2014 is painted as markup so it stands out from the spec text around it, wherever it appears: in steps, titles, descriptions, table cells and doc strings. Where two patterns match the same text, the one declared first wins, matching the order SpecBinder applies them in. The colour is configurable under Settings &gt; Editor &gt; Color Scheme &gt; Gherkin.","A step\'s Playwright trace can now be opened in your browser from the trace row itself. Pointing at the row reveals a small arrow beside the trace\'s file name; clicking it opens that same recording full-size in Playwright\'s own trace viewer, while the row\'s file name still expands the viewer inline as before. The recording is handed over on a local address and never leaves your machine. Previously this was only reachable by Shift- or Alt-clicking the file name, which still works.","Ctrl/Cmd+Click on a step carrying revision markup now opens the same step method as the plain step would: the method name is worked out from the step text with the markup stripped, so annotating a step for traceability never costs you the jump to its implementation.","Find Usages on a step method now lists the spec steps that only match once their revision markup is stripped, so a step annotated for traceability is not silently missing from the usage list.","A range of spec text can now be marked by its two ends, using the <code>stripBetweenPatterns</code> option: one pattern for where the range opens and one for where it closes. Everything from the opening marker to the closing one is treated as stripped, so a range can wrap whole steps, a whole scenario or rows of a table without either pattern needing to match across lines. Each opening marker is closed by the nearest following one, so a marker written inside a range is part of what the range covers; an opening marker with nothing closing it leaves the text exactly as you wrote it, rather than swallowing the rest of the file. Ranges are applied before the <code>stripPatterns</code> entries, so a range keeps its whole span even when an entry would also have matched inside it.","Inside a marked range, only the two markers are highlighted \u2014 the spec text between them keeps its ordinary colouring and still reads as Gherkin, so a range wrapping a whole scenario looks like a scenario rather than a block of markup. Everything in the range is still stripped before the tests are generated, so the names, numbering and recorded status of what survives are worked out as if the range were not there: a scenario that gains a range around some of its steps keeps the result already recorded against it.","The text inside a marked range can now be given a look of its own, through a new <strong>Stripped range content</strong> entry under Settings &gt; Editor &gt; Color Scheme &gt; Gherkin. It starts with nothing set, which leaves a range exactly as it reads today. Because each attribute of the entry is set on its own, a style chosen without a colour is laid over the range rather than replacing it \u2014 tick Effects &gt; Strikeout and the wrapped text keeps its ordinary Gherkin colours with a line through it, which is a natural way to show wording that no longer reaches the tests. Choosing a foreground instead colours the range outright.","A range\'s two markers can now be coloured apart from a plain <code>stripPatterns</code> match, through a new <strong>Stripped range marker</strong> entry. It takes its colours from <strong>Strip pattern</strong> unless you set it, so nothing changes until you ask for it \u2014 it is there for authors who want \\"this whole span is going\\" to look different from \\"these tags are going\\".","An unusable <code>stripBetweenPatterns</code> pair is now reported in the editor on the offending pattern \u2014 whichever of the two will not compile, or either one left blank, since a pair with only one end cannot mark a range. Until the pair is usable the spec file is highlighted as if no ranges were configured, rather than guessing where you meant the range to close.","An unparseable <code>stripPatterns</code> entry is now reported in the editor, on the offending pattern itself, so you find out as you type instead of from a failed build. Until it compiles, the spec file is highlighted as if no patterns were configured.","Renaming a step method now updates the matching step in the spec file even when that step carries revision markup: the step is found by its text with the markup stripped, so annotating a step for traceability no longer leaves it behind, out of sync with the method it runs. The rewritten step is written plain \u2014 a method name records nothing about where the markers sat \u2014 and text a pair of markers wraps and removes goes with them. A marker no configured pattern matches is part of the step text the method name was built from, so it stays put: only the words the rename actually changed are rewritten, leaving the rest of the line exactly as you wrote it.","Step completion now suggests steps for a line carrying revision markup: the text you have typed is matched with the markup stripped, so annotating a step part way through typing no longer empties the suggestion list. Text wrapped by a pair of markers is dropped along with them, exactly as it is when the tests are generated.","Ctrl/Cmd+Click from generated code back to the spec now lands on the right place when revision markup is involved. Shift+Ctrl/Cmd+Click on a step method call opens the marked-up step it was generated from, and where two steps read alike once their markup is stripped, each call opens its own step rather than both opening the first. Ctrl/Cmd+Click on a generated test method or nested class opens the Scenario or Rule it came from even when that element\'s title carries markup \u2014 titles are matched with the markup stripped, the way SpecBinder writes them into the generated <code>@DisplayName</code>.","A scenario keeps its recorded execution status when you annotate it with revision markup. The scenario is matched to its recorded run by its content with the markup stripped \u2014 the same way SpecBinder records it \u2014 so adding traceability markup no longer blanks the status icon in the gutter, and a scenario beside an annotated one is unaffected. Editing what a step actually says still invalidates the match: the scenario then reads as not executed until the tests are run again."]},{"type":"Changed","items":["Reformat Code now pulls doc string lines that start to the left of the opening triple quotes back in line with them, instead of leaving them hanging outside the block. Lines that already start at or beyond the triple quotes keep their own indentation relative to the block, and blank lines are left untouched.","A Playwright trace opened in your browser is now named after its own recording file, so a tab full of traces can be told apart. Previously every trace arrived under the same meaningless identifier, leaving the browser tab and the viewer\'s own heading identical for each one.","The doc-string previews and published reporter entries buttons on the editor toolbar now set their panels\' left-bar colour from a single \\"Panel bar colour\\" swatch in their dropdown, opening the full colour picker in one click. This matches how the failed-step error panels dropdown already worked, so every panel colour is chosen the same way. The grid of preset colours and the separate \\"Custom\u2026\\" button are gone from both dropdowns; a colour picked previously is kept."]},{"type":"Fixed","items":["The execution report status counts now retire a scenario that was changed outside the IDE. A spec file rewritten on disk by another tool, a script, or a branch switch never saves through the editor, so its edited scenarios kept their recorded pass or fail; they are now counted as not executed as soon as the change reaches the file, exactly as they are after an in-editor save. Scenarios the change left alone keep their recorded status, and restoring the original content brings the recorded status back.","Scenarios and Rules that follow spec text removed by revision markup are no longer mapped onto the wrong generated test. A scenario after a removed one is numbered the way SpecBinder numbers it, so its gutter Run icon starts the right test, Ctrl/Cmd+Click from the generated class lands on the right scenario, Find Usages lists the right steps, and execution report statuses are drawn against the scenario that produced them.","Reformat Code now leaves a line that begins with revision markup exactly where it is. Previously a table row wrapped in markup was pushed out to the left margin; it now keeps its indentation and its own spacing, while the rows around it are aligned as if it were not there.","The spell checker no longer flags revision markup as typos, while still checking the words the markup wraps.","A spec file that was already open while the project was still loading could show \\"No execution report\\" for the rest of the session, even after the spec had been run and its report was on disk. The report is now picked up as soon as it is written.","The inline preview of an SVG doc string now draws the graphic on a high-resolution display. The preview opened at the right size but painted little or nothing of the picture, so a fine-lined graphic looked like an empty panel. The image preview is drawn the same way.","Approving a failed step\'s Actual value no longer stops the other open error panels from approving theirs. Accepting a value makes the spec file longer and pushes every step below it further down, and the panels still open carried on pointing at the lines their steps used to occupy \u2014 so from the second approval onwards the button reported \\"Cannot locate the expected value as a parameter on this step\\", and a run with several failures had to be approved one panel at a time, closing and reopening each. Each panel now follows the step it was opened on however far that step has moved, so the failures can be worked through in a single pass, in any order. Editing the file by hand while panels are open is handled the same way, and a failed step\'s gutter icon still closes that step\'s own panel after the step has shifted."]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1156641"},{"version":"2026.16.0","date":"2026-07-31","unreleased":false,"sections":[{"type":"Changed","items":["The Step Implementation tool window no longer draws an icon in the loaded editor\'s gutter next to the linked method. The highlighted line already marks it, and dropping the icon keeps the gutter at its normal width."]},{"type":"Fixed","items":["The Step Implementation tool window now reliably scrolls the linked method into view when it loads a class: previously the newly loaded file could appear scrolled to the top instead of centred on the method. The loaded file is also kept aligned to the left edge, so it no longer scrolls sideways to reveal the indented method name.","The step implementation preview (Quick Documentation on a step) and the Step Implementation tool window now show the actual step method body \u2014 the concrete implementation in your test class \u2014 instead of the empty abstract declaration, for steps whose implementation lives in a subclass of the generated test class. Ctrl/Cmd+Click on a step lands on the same concrete implementation."]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1122307"},{"version":"2026.15.0","date":"2026-07-31","unreleased":false,"sections":[{"type":"Fixed","items":["The step implementation preview (Quick Documentation on a step) and the Step Implementation tool window now show the actual step method body \u2014 the concrete implementation in your test class \u2014 instead of the empty abstract declaration, for steps whose implementation lives in a subclass of the generated test class. Ctrl/Cmd+Click on a step lands on the same concrete implementation."]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1121961"},{"version":"2026.14.0","date":"2026-07-31","unreleased":false,"sections":[{"type":"Added","items":["A new <strong>Auto-Enable Remote Control (Claude Code only)</strong> toggle in the Launch AI agent button\'s dropdown (below <strong>Auto-Select This Spec\'s Terminal Tab</strong>, off by default and remembered). When on, Claude Code sessions you start from the button launch with remote control enabled, so you can drive or watch each session from claude.ai or the mobile app without turning it on by hand. It only affects Claude Code launches \u2014 Copilot CLI sessions are unaffected \u2014 and whether a session actually connects for remote control depends on your Claude sign-in.","A new <strong>Step Implementation</strong> tool window (right dock) shows the Java implementation of the step under your caret in a spec file, and follows the caret: as you move through steps, it loads the step method\'s owning class and scrolls to that method, highlighting the linked method\'s line with a gutter marker so it stands out. It hosts a real editor, so you can read, edit, and refactor the class directly in the pane; the pane header shows the loaded file\'s name and icon, and a colour swatch there lets you choose the highlight colour for the linked method. When the caret is not on a step, nothing resolves, or the focused file is not a spec, it simply keeps showing the last class rather than blanking. It resolves to the same method Ctrl/Cmd+Click and Quick Documentation open.","Quick Documentation (Cmd+Q) on a step in a spec file now previews that step\'s Java implementation inline in the documentation popup, syntax-highlighted, so you can review what a step does without jumping to the generated test class. It appears only when you invoke the shortcut \u2014 not automatically on mouse-hover \u2014 and resolves to the same method Ctrl/Cmd+Click navigates to. When a step has no implementation yet (its generated test class isn\'t compiled, or no matching class was found), the popup says so instead of showing nothing.","When a step\'s execution recorded a Playwright trace, an expandable <strong>Playwright trace</strong> row now appears beneath that step \u2014 in both the spec editor and the Execution Report preview \u2014 alongside any published-reporter entries and controlled by the same toolbar toggle that shows those entries. Clicking the row opens the recorded trace inline in the embedded Playwright trace viewer (screenshots, DOM snapshots, network, console, and the action timeline); collapsing it releases the viewer. Shift- or Alt-clicking the row instead opens the trace full-size in your system browser. The viewer loads from trace.playwright.dev, so it needs an internet connection \u2014 the trace file itself stays on your machine and is served to the viewer locally. The trace is matched to its step from the recorded report, so the row keeps showing the run even after the spec file is edited.","New \\"Show Playwright traces\\" toggle (Execution results, nested under \\"Show published JUnit reporter entries\\", off by default) controls whether that recorded trace becomes the expandable trace-viewer row. When turned off, the trace is instead shown as an ordinary published-reporter entry \u2014 a plain <code>specbinder.playwright.trace: &lt;path&gt;</code> row with no embedded viewer.","Doc strings opened with an <code>\\"\\"\\"xhtml</code> fence are now treated the same as <code>\\"\\"\\"html</code> ones: their body gets HTML syntax highlighting, the body is foldable (and honours the \\"Collapse HTML doc strings\\" setting), a gutter icon renders an inline preview of the markup, and selecting the body and running Reformat Code pretty-prints it with the HTML formatter.","Selecting the body lines of a typed doc string (one opened with a content-type fence such as <code>\\"\\"\\"json</code> or <code>\\"\\"\\"html</code>) and running Reformat Code now pretty-prints that content in its own language \u2014 JSON, YAML, HTML, XML, and SVG \u2014 and re-indents the result under the doc string\'s fence. Only the selected doc string is affected: reformatting with nothing selected (or the caret elsewhere in the file) leaves every doc-string body untouched, and a doc string with no content type, or one whose type has no formatter, is left unchanged.","The Execution Report preview now reproduces a Scenario Outline (or Scenario Template) by rebuilding its heading and placeholder steps, then listing one nested <code>Example</code> per row of its examples table with each row\'s values substituted into the step text. The rebuilt heading and placeholder steps come from the recorded report; the <code>Examples</code> table itself is not repeated. Reports produced without the outline\'s template steps (older runs) instead show the substituted rows alone, without a rebuilt heading. As with the rest of the preview, the rows come from the recorded report, so the pane keeps showing the run even after the spec file is edited.","A doc string or data table attached to a Scenario Outline step is now reproduced in the Execution Report preview too: it appears once beneath the rebuilt placeholder step with its <code>&lt;&gt;</code> placeholders left in place, and again beneath each expanded <code>Example</code> with that row\'s values substituted in. As with the outline\'s step text, both come from the recorded report.","The active-pair link highlight now pairs a Scenario Outline with the template block the Execution Report pane rebuilds for it: putting the caret on the outline heading frames the rebuilt heading together with any description lines beneath it, on a Scenario Outline step frames its rebuilt placeholder step together with any doc string or data table attached to it, and on an examples table row frames just that row\'s expanded <code>Example</code> \u2014 each with a connecting divider polygon. The pairing works both ways: in the report pane, putting the caret on a rebuilt <code>Example</code> heading frames the examples table row it was expanded from, on a rebuilt placeholder step frames the outline step in the editor, and on a step inside an expanded <code>Example</code> frames the outline template step that row ran from. The <code>Examples</code> keyword and the table header row have no counterpart, so they highlight nothing.","The <code>&lt;&gt;</code> placeholders in a Scenario Outline\'s rebuilt heading and template steps now carry the same outline-parameter colour in the Execution Report preview that they have in the spec editor, so the parameterised template reads the same on both sides.","The active-pair link highlight now links a <code>Background</code> step with the copies the Execution Report pane inlines ahead of each scenario. Because a background step is repeated once per scenario in the report, putting the caret on the <code>Background</code> heading or one of its steps in the editor highlights nothing; instead, putting the caret on one of those inlined report lines frames just that line and, across the divider, the single <code>Background</code> step in the editor it came from \u2014 even when other scenarios inline the same step.","The Execution Report preview now shows each scenario\'s Background steps inline as its leading steps \u2014 the feature-level, and for a scenario under a Rule the rule-level, steps that ran before it, in run order \u2014 taken from the recorded report. There is no separate <code>Background</code> heading in the preview; the steps simply precede the scenario\'s own steps. This also applies to the per-row scenarios a Scenario Outline expands into.","Changing a Java step method\'s parameters with IntelliJ\'s Change Signature refactoring now updates the matching step text in associated feature files. Adding a parameter inserts the default value you give it as a quoted value at the corresponding position in each step; removing a parameter drops its quoted value (keeping any doc string or data table attached to the step); reordering parameters rearranges the quoted values to match the new order. Each quoted value follows its parameter, so this works even when the same change also renames the method."]},{"type":"Changed","items":["Every option on the Execution results settings page now starts turned off, so all execution-report-driven features are opt-in: gutter and toolbar status indicators, failed-step error icons and their inline panel, Project-view spec file icons and folder status chevrons, the published-reporter-entry panels (and Playwright traces), and the Execution Report split view. Turn on the ones you want under Settings \u2192 Tools \u2192 SpecBinder \u2192 Execution results.","The editor \\"Collapse on open\\" folding option for HTML doc strings is now labelled \\"HTML/XHTML doc strings\\", reflecting that it also collapses <code>\\"\\"\\"xhtml</code> doc strings on open.","Outline placeholders (<code>&lt;name&gt;</code>) inside a Scenario Outline are now always shown in the single outline-parameter colour, whether or not the placeholder\'s name matches a column in the Examples table. The separate colour previously used for an unmatched placeholder has been removed, along with its \\"unresolved outline parameter\\" entry in Settings \u2192 Editor \u2192 Color Scheme \u2192 Gherkin.","Navigation, Find Usages, and step completion now recognise the renamed feature-level <code>Background</code> setup method in SpecBinder-generated test code, keeping Ctrl/Cmd+Click and Find Usages between a feature file\'s <code>Background</code> and its generated method working with the current generator output.","Ctrl/Cmd+Click on a step now goes to the step method itself in the generated test \u2014 its concrete implementation, or the abstract declaration when there is no implementation yet \u2014 and Ctrl/Cmd+Click on a step\'s quoted parameter, doc string, or data table goes to the matching parameter of that method. To jump to the step\'s call site inside the generated test method instead (the previous Ctrl/Cmd+Click destination), use Ctrl/Cmd+Shift+Click, which mirrors Ctrl/Cmd+Shift+Click from a generated call site back to its step. Both directions show the usual navigation link on hover. Call-site navigation lands on the whole step\'s call statement, not an individual argument."]},{"type":"Fixed","items":["The agent status badge (the diamond drawn over a spec file\'s icon to show whether its linked agent is working, needs input, or has finished) now appears on a freshly installed IDE. Previously, on a machine where the plugin had not yet created its internal status folder, the launched agent\'s state was never recorded and no badge ever showed; the folder is now created when an agent is launched, and each state update recreates it if it has since been removed, so the badge lights up reliably.","The inline Playwright trace row no longer traps the editor\'s vertical scroll: hovering the collapsed row and using the mouse wheel now scrolls the editor as usual (previously scrolling stopped until the pointer moved off the row). The expanded trace viewer keeps its own scrolling, and once its content reaches the top or bottom the wheel continues scrolling the surrounding editor. The row also shows the normal pointer instead of a text cursor.","The failed-step error icon on a Scenario Outline\'s expanded example steps in the Execution Report pane is now clickable: clicking it (or opening all panels from the toolbar\'s error-panels button) reveals the inline error panel with that example\'s failure detail beneath the step, matching how it already works for a plain scenario\'s failed steps. Previously the icon appeared but did nothing. When the example step recorded Expected and Actual values, the panel also offers the Show diff (and, for HTML or image doc-string values, Visual diff) actions; Approve is not offered for an example step, because its expected value lives in the shared outline template.","Angle-bracket text such as <code>&lt;name&gt;</code> is now highlighted as an outline placeholder only where it can actually be one: inside a Scenario Outline or Scenario Template, and never inside an <code>html</code>, <code>xml</code>, or <code>svg</code> doc string (where <code>&lt;\u2026&gt;</code> is the markup\'s own tags). In a plain Scenario, a Background, or a markup doc string the same <code>&lt;\u2026&gt;</code> is shown as ordinary text \u2014 step text, a literal table value, or plain doc-string content \u2014 instead of the bold outline-parameter styling.","Report-driven gutter status icons now treat a Scenario Outline\'s example rows that the execution report never recorded as not executed: such a row, the Scenario Outline heading, and the Rule and Feature above it show the amber not-executed chevron instead of leaving the row without an icon and the heading passed. An outline that already has a failed row still shows failed.","Find Usages on a Java step method now lists its occurrences in steps belonging to scenarios nested under a Rule, not only scenarios at the top level of a spec file.","Reduced the chance of a spurious \\"stub index\\" error being reported when opening a project in a Gradle module, by reusing a cached lookup for the spec file\'s generated test class instead of re-running a project-wide search on every toolbar refresh.","Enabling, disabling, or updating the plugin now prompts to restart the IDE instead of reloading it in place. This avoids a rare crash where opening a spec file after re-enabling the plugin failed with an internal class-loading error.","Pressing Esc while a failed step\'s <strong>Visual diff</strong> window (the rendered HTML comparison or the image comparison) is open now closes it and returns to the editor, matching how the standalone text diff window already behaved. Previously Esc only refocused the main IDE window and left the Visual diff window open."]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1121806"},{"version":"2026.13.0","date":"2026-07-12","unreleased":false,"sections":[{"type":"Added","items":["The left spec-editor toolbar now has a report section (after the collapse / expand buttons) showing the date and time the displayed Execution Report was generated, with an \\"Auto-reload\\" checkbox and a \\"Reload\\" link. With auto-reload off (the default) the pane keeps showing the current report when a newer one is written to disk and the \\"Reload\\" link appears to load it on demand; with auto-reload on, newer reports load automatically (the setting is global and also lives in Settings &gt; Tools &gt; SpecBinder &gt; Execution results). Switching to editor-only view and back to a report view reloads the latest report automatically.","Every right-pane colour palette (failed-step error panels, image and HTML doc-string previews, published reporter entries, and the split-view link highlight) now shows a swatch of your last-picked custom colour next to the \\"Custom\u2026\\" button, so the custom colour stays visible at a glance; clicking the swatch re-applies it.","Two new buttons on the left spec-editor toolbar (after the status lozenges) collapse or expand every Scenario and Background section in the file at once. They act on whichever editors are visible \u2014 both the spec editor and the Execution Report pane in split view, the spec editor alone in editor-only mode, and the report pane alone in preview-only mode.","A new \\"Show Previews\\" split button on the right-pane toolbar: its main button opens the inline preview for every previewable doc string in the file at once \u2014 image, HTML, and SVG alike \u2014 and clicking it again closes them; its dropdown arrow opens a colour palette (with a \\"Custom\u2026\\" option) for the single shared colour of each preview\'s left bar. It acts on whichever editors are visible \u2014 both panes in split view, the spec editor in editor-only mode, the report pane in preview-only mode.","A new \\"show all error panels\\" split button on the right-pane toolbar (second from the left): its main button opens an inline error panel under every failed step at once, and clicking it again closes them all; its dropdown arrow opens a colour palette (with a \\"Custom\u2026\\" option) for the colour of each panel\'s left bar. The panels appear in the Execution Report pane in split/preview modes and in the spec editor in editor-only mode. Whether inline error panels are available at all remains controlled by the SpecBinder settings.","New master \\"Use SpecBinder execution reports\\" checkbox (Execution results): when off, no execution-report data is read and all dependent features (gutter icons, failed-step error icons, inline error panel, split-view) are disabled. On by default.","New \\"Show inline error panel\\" toggle (Execution results): when on (default), clicking a failed-step error icon opens the inline error panel; when off, the icon is indicator-only.","New \\"Show HTML previews\\" and \\"Show image previews\\" toggles for the inline error panel\'s Expected / Actual values (both on by default).","The SpecBinder landing page now has shortcut links that jump to the Gherkin \\"Color scheme\\" and \\"Code style\\" pages (under the IDE\'s standard Editor sections).","Long lines in spec files can be kept on one line with a horizontal scrollbar instead of wrapping (\\"Don\'t wrap long lines in spec files\\", under Editor); also applies to the Execution Report view in split mode, and takes effect on next file open.","Clicking the gutter Run icon on a Feature, Rule, or Scenario keyword in a <code>.specb</code> / <code>.feature</code> file now creates a Gradle test run configuration (equivalent to <code>./gradlew :test --tests \\"&lt;class&gt;\\"</code> or <code>\u2026--tests \\"&lt;class&gt;.&lt;method&gt;\\"</code>) when the file lives in a Gradle-managed module, so test execution flows through the Gradle build with the matching task graph, system properties, and IDE / build classpath rather than IntelliJ\'s direct JUnit runner. Maven and non-Gradle projects continue to use the existing JUnit run configuration.","The Execution Report preview now shows data tables and doc strings inline beneath each step, formatted in the same Gherkin shape as the source file (pipe-aligned table rows; triple-quoted doc string blocks)","The Execution Report preview now renders Feature, Rule, and Scenario description text inline beneath each heading, mirroring its position in the source file. Description text is taken from the JSON report when the test was generated with description-as-annotation support, and is silently omitted for older reports","A toolbar above the left editor pane shows coloured execution-status lozenges for the current Execution Report \u2014 an overall-status pill plus passed / failed / skipped / unexecuted scenario counts (a status with no scenarios is hidden). Clicking a count lozenge moves the caret to the next scenario of that status, wrapping after the last; clicking the overall pill steps through the scenarios that share the run\'s overall status","A toolbar above the right Execution Report pane provides a toggle for showing or hiding published reporter entries (off by default)","The right-pane toolbar now has a toggle for synchronising the Execution Report\'s scroll position with the editor (on by default); when on, scrolling either pane drives the other","The right-pane toolbar now has a toggle for showing the active-pair link highlight \u2014 the divider polygon together with the line tints on either pane \u2014 on by default; the toggle disables itself when the editor is in editor-only or preview-only mode where there\'s no divider to draw into","The right-pane toolbar now has a colour swatch button that opens a colour picker for the active-pair link colour used by the divider polygon and the line tints on either pane","Click the red error icon in the Execution Report pane\'s gutter next to a failed step to toggle an inline error panel showing the message, Expected vs Actual, and stack trace \u2014 the same affordance available on the spec editor pane, with independent open / closed state per pane","The body of a plain <code>\\"\\"\\"</code> doc string (no media-type identifier) is now foldable from the editor gutter, the same way HTML / JSON / XML / YAML doc strings already are. A new \\"Collapse plain doc strings\\" checkbox under Settings &gt; Tools &gt; SpecBinder &gt; Code folding controls whether plain doc-string bodies are collapsed by default when a feature file opens.","Doc-string bodies are now foldable in the Execution Report preview pane too, mirroring the editor-side behaviour: every doc string is foldable regardless of its media-type identifier, and the initial collapsed state respects the matching \\"Collapse \u2026 doc strings\\" setting (plain, HTML, JSON, XML, or YAML).","The HTML doc-string preview gutter icon now appears in the Execution Report preview pane next to every <code>\\"\\"\\"html</code> opening fence. Clicking it toggles the same inline rendered preview beneath the closing fence that the editor pane already offers.","The image doc-string preview gutter icon now appears in the Execution Report preview pane next to every <code>\\"\\"\\"image</code> (and <code>\\"\\"\\"img</code>) opening fence. Clicking it toggles the same inline rendered image preview beneath the closing fence that the editor pane already offers.","A new <code>\\"\\"\\"image</code> content-type identifier for doc strings: clicking the gutter preview icon next to a <code>\\"\\"\\"image</code> opening fence toggles an inline rendered preview of the base64-encoded image (PNG, JPEG, GIF, WebP, BMP \u2014 format auto-detected from the decoded bytes) beneath the closing fence, with the same thin frame, step-keyword-aligned indent, and editor background as the <code>\\"\\"\\"html</code> preview. An empty body, invalid base64, or bytes that aren\'t a recognised image format surface a red error icon and explanatory message in place of the preview. Spell-checking is automatically disabled inside <code>\\"\\"\\"image</code> bodies so the IDE\'s typo quick-fix can\'t silently corrupt the encoded bytes.","Two new checkboxes under Settings &gt; Tools &gt; SpecBinder for the <code>\\"\\"\\"image</code> doc-string family: \\"Auto-open image previews\\" (under Doc string previews) renders every image preview automatically when a feature file opens, and \\"Collapse image doc strings\\" (under Code folding) folds <code>\\"\\"\\"image</code> (and <code>\\"\\"\\"img</code>) doc-string bodies by default \u2014 both mirror the equivalent toggles already available for <code>\\"\\"\\"html</code> doc strings.","When a step fails comparing two <code>\\"\\"\\"image</code> (or <code>\\"\\"\\"img</code>) doc-string values, the inline error panel now embeds a decoded image preview beneath each of the Expected and Actual base64 rows, the same way it already does for <code>\\"\\"\\"html</code> doc strings, so the two images can be compared side by side without copy-pasting the base64 into an external viewer.","The inline error panel\'s action row now treats <code>\\"\\"\\"image</code> (and <code>\\"\\"\\"img</code>) doc-string parameters the same way as <code>\\"\\"\\"html</code>: the Approve button rewrites the failing step\'s doc-string body with the Actual base64 verbatim, and the Show diff button opens the textual diff viewer. The Visual diff button is omitted for <code>\\"\\"\\"image</code> step parameters (image rendering is already shown inline beneath each value row)."]},{"type":"Changed","items":["The Execution Report preview now renders Feature, Rule, Scenario, and step rows from the text recorded in the JSON report \u2014 Scenario Outline rows show parameter values exactly as they were executed, and the preview keeps working when the source file has uncommitted edits since the run. Older reports continue to render using the source file\'s text as a fallback.","Failed-step gutter icons now use a red circle with an X cut-out, visually distinct from the red circle with a white exclamation mark used on the Execution Report preview\'s aggregate failure rows for Feature, Rule, and Scenario \u2014 making it easy to tell at a glance which row is the actual failing step (and the clickable affordance for the inline error panel) versus the rows that summarise a descendant failure","When the caret sits anywhere inside a step block \u2014 its keyword line, its data table rows, or its doc string content \u2014 the active-pair link (line tint on both panes and connector polygon in the divider) now spans the full step block on both sides, rather than just the keyword line","When the caret sits on a description line of a Feature, Rule, or Scenario \u2014 or on its heading \u2014 the active-pair link now spans the full heading-plus-description block on both panes, the same way step blocks already widen for their data tables and doc strings","Published reporter entries (the indented \\"\u21b3 key: value\\" rows under steps) are now hidden by default in the Execution Report preview and can be turned on from the new right-pane toolbar toggle","The Execution Report preview now shows DocString media-type identifiers (e.g. <code>\\"\\"\\"html</code>) on the opening fence and uses source-verbatim column headings (e.g. <code>| User Name |</code>) for data tables, matching how the spec was originally written rather than the sanitized field names used inside the row data","The Execution Report preview now applies the same per-language doc string support as the editor: a <code>\\"\\"\\"html</code> body is rendered with HTML syntax highlighting (and other supported media-type identifiers render with their respective languages). The run / re-run gutter icons remain editor-only and are not painted in the report preview","When a failed step has a doc string or data table argument, the inline error panel is now rendered immediately below that argument block instead of between the step keyword line and the argument rows, so the step and its argument stay visually together. This applies to both the spec editor pane and the Execution Report preview","Published reporter entries in the Execution Report preview are now shown in a dedicated inline panel under the step (a block with a teal left stripe) instead of inserting extra lines into the preview. Entry text inside the panel is selectable and can be copied like any other editor content, the panel adds vertical breathing space above, below, and between individual entries so the rows do not look squashed, the text scales with the editor\'s font zoom (Cmd / Ctrl + scroll or Increase / Decrease Font Size actions) to stay consistent with the surrounding right-pane text, the right-pane toolbar toggle still controls whether these panels are visible, the entry rendering rules and ordering are unchanged, and on a failed step the published-entries panel sits between the step row and the inline error panel so the contextual data the step recorded is read before the failure detail","The inline error panel for failed steps now scales with the editor\'s font zoom \u2014 increasing or decreasing the font size in the spec editor or the Execution Report preview also resizes the panel\'s Message, Expected, Actual, and stack-trace content text, so it stays consistent with the surrounding editor text on both panes","The inline panels under steps \u2014 the failed-step error panel on both panes and the published reporter entries panel on the Execution Report preview \u2014 are now indented so their coloured left stripe sits two columns to the right of the owning step keyword instead of starting flush against the editor\'s left edge. The indent stays aligned with the step keyword\'s column at every font zoom level, and in both single-pane editor and split editor / preview view modes","The inline rendered HTML preview for <code>\\"\\"\\"html</code> doc strings now uses the same step-keyword-aligned horizontal indent on both panes, and stays aligned in both single-pane editor and split editor / preview view modes; when the step also has published reporter entries and / or a failed-step error panel, the HTML preview is rendered first immediately under the doc-string closing fence, with the published-entries panel below it and the error panel at the bottom","The inline rendered HTML preview panel for <code>\\"\\"\\"html</code> doc strings now uses the editor\'s own background colour around its embedded browser \u2014 replacing the previous subtle grey panel background \u2014 so the preview blends seamlessly into the surrounding editor on both the spec editor and the Execution Report preview, in all view modes (editor-only, preview-only, and split editor / preview)","The inline rendered HTML preview panel for <code>\\"\\"\\"html</code> doc strings now draws a thin frame around its embedded browser, matching the frame the failed-step inline error panel draws around its Expected / Actual HTML previews, so both surfaces present rendered HTML with the same visual affordance","The toolbar row above the editor (failed-scenario counter + prev / next arrows on the left, rendering-option toggles on the right) is now shown in editor-only view as well, not only in split editor / preview and preview-only views \u2014 so the failed-scenario navigation arrows and the preview-related toggles remain reachable without having to switch the view mode first","The blank vertical strip between the spec editor and the Execution Report preview in split editor / preview view is now half as wide, giving more horizontal room to the editor panes themselves while still leaving space for the active-pair link connector polygon"]},{"type":"Fixed","items":["The active-pair link connector polygon in the split editor\'s divider no longer drifts out of alignment with the line tints on either pane after clicking inside an inline panel (published reporter entries, error panel, HTML preview) or after focus shifts between panes without a caret move; the polygon and the line tints now always reflect the same anchor element","Resolved an internal IDE exception that was raised on every repaint of the split editor\'s divider when the Execution Report preview was open","Opening a <code>.specb</code> or <code>.feature</code> file in split view no longer occasionally leaves the editor\'s gutter on its default left side; it now consistently sits next to the central divider from the first paint, without requiring a manual toggle of the view mode to fix it","The Execution Report preview no longer shows two stacked HTML preview toggle icons next to <code>\\"\\"\\"html</code> doc string fences; only the preview\'s own native toggle (wired to the right pane\'s inlay) is shown","Hovering the HTML preview toggle in the Execution Report preview\'s gutter now changes the mouse pointer to the hand cursor, matching the affordance shown for the same icon on the editor side","The \\"Message\\", \\"Expected\\", \\"Actual\\", and \\"Stack trace\\" field labels in the inline error panel for failed steps now use the same font size as their values, instead of the smaller UI font that made the labels read as a different rank","Published reporter entries in the Execution Report preview no longer occasionally appear twice under the same step; each step\'s published-entries panel is now installed exactly once","The inline error panel\'s action buttons (Approve, Show diff, Visual diff) and the diff viewers they open no longer fail silently in the released plugin \u2014 internal helper classes are now correctly preserved through obfuscation so the panel and its actions load reliably"]},{"type":"Removed","items":["The \\"Fold long values\\" setting (fold mode + line-count threshold) has been removed from the inline error panel options. Long values now always start collapsed to their first line \u2014 every multi-line value, plus any single line longer than 140 characters \u2014 with a chevron to expand. This is no longer configurable."]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1105203"},{"version":"2026.11.0","date":"2026-05-26","unreleased":false,"sections":[{"type":"Removed","items":["Compatibility with IntelliJ IDEA 2024.2 \u2014 the minimum supported IDE build is now 2024.3 (243.0). Users still on 2024.2 should upgrade their IDE to install future versions of the plugin."]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1057407"},{"version":"2026.10.0","date":"2026-05-25","unreleased":false,"sections":[{"type":"Added","items":["In the inline error panel, the cursor now reads as a hand-pointer when hovering the chevron and as a text I-beam when hovering the value text, so clickable and selectable regions of a Message, Expected, Actual, or Stack trace row are visually distinct at a glance.","The inline error panel\'s Stack trace now mirrors the same row layout as the Message, Expected, and Actual rows: the first line of the stack trace (typically the exception class plus message) is shown in the row header alongside a chevron, and the remaining frames are revealed when the row is expanded. Single-line stack traces render plainly with no chevron.","Navigation support for Gherkin keyword aliases: Ability, Business Need, Narrative (Feature), Example (Scenario), and Scenario Template (Scenario Outline)","Code Style settings page under Editor &gt; Code Style &gt; Gherkin (SpecBinder) with configurable tabs and indents, commenting, and blank line options","Step auto-completion now uses fuzzy word matching: typing any word will suggest every step whose text contains that word at any position; typing multiple words requires every word to appear in the step text, in any order","Gutter icons in feature files now reflect the latest execution state of the corresponding test: Feature, Rule, and Scenario keywords show a green check when the matching test has passed, or a red mark when the matching test has failed","Breakpoints can now be set directly on Gherkin step lines in feature files; running the test in debug mode pauses the JVM at the corresponding generated call site, and the editor highlights the step in the feature file rather than the generated Java source","HTML doc-string parameters (opened with <code>\\"\\"\\"html</code>) now show a preview icon in the editor gutter on the line of the opening fence","Clicking the HTML doc-string gutter preview icon displays an inline preview of the rendered markup below the closing fence of the doc string; clicking it again removes the preview","Open HTML doc-string previews update automatically and live-reload as you edit the markup, with rapid keystrokes debounced to avoid flicker","Gutter icons can now reflect per-scenario pass/fail outcomes recorded by the SpecBinder execution reporter\'s JSON files, including per-row icons on Scenario Outline data rows; failing scenarios get a tooltip naming the failing step","When the recorded outcome no longer matches the current spec content (e.g. the scenario was edited since the last run), the stale pass/fail icon is hidden by default so a green checkmark cannot mislead; an opt-in setting renders the stale outcome as a desaturated icon with a warning overlay instead","New \\"JSON-report results\\" section under Settings &gt; Tools &gt; SpecBinder with two checkboxes: a master toggle (off by default) to enable JSON-based gutter icons, and a \\"Hide stale icons\\" toggle (on by default) to control how stale outcomes are visualised","Failed steps now also show a red error icon in the gutter, anchored on the step keyword, with a tooltip carrying the recorded error type and message; only steps recorded as failed are decorated, passed and skipped steps remain untouched","Failed-step gutter icons are also subject to the existing staleness rules: when the recorded outcome no longer matches the current spec content (e.g. the scenario was edited since the last run), the failed-step icon is hidden by default so a stale red error icon cannot mislead","Stack-trace frames in the inline error panel are now clickable hyperlinks: each <code>(FileName.java:line)</code> reference becomes a link that opens the source file at the given line, mirroring the navigation experience of IntelliJ\'s run/debug console output. Frames without a navigable source location (Native Method, Unknown Source) stay as plain text. Frames pointing at a nested class (e.g. a SpecBinder-generated <code>@Nested</code> Rule class) navigate correctly even though the stack trace uses the JVM <code>Outer$Inner</code> form","Stack-trace frames whose class lives outside the project\'s source files (JDK, JUnit, and other library frames) are now rendered in a softened, lower-contrast colour, while frames pointing at the project\'s own source files are rendered with a teal accent in bold by default \u2014 together making user-code frames immediately spottable in long stack traces, matching the IntelliJ run/debug console convention","New \\"Inline error panel \u2014 in-module stack trace styling\\" section under Settings &gt; Tools &gt; SpecBinder lets you pick the colour and font style (Normal / Italic / Bold) used for in-module stack trace frames in the inline error panel","Doc strings tagged with an HTML fence (e.g. <code>\\"\\"\\"html</code>) are now treated as HTML by the editor: the contained markup gets HTML syntax highlighting, fold markers on multi-line elements, and HTML completion proposals \u2014 the same language services you would see in a standalone <code>.html</code> file. The fence identifier and surrounding Gherkin keep their normal Gherkin colours, and the doc-string indentation is removed before the content is handed to the HTML language services so embedded markup is parsed as if written at column zero. Editing inside the highlighted region is read-only in this release.","New \\"Collapse HTML doc strings\\" option under Settings &gt; Tools &gt; SpecBinder &gt; Code folding: when enabled, the body of every <code>\\"\\"\\"html</code> doc string is folded by default when a feature file is opened, mirroring the existing Collapse Rules / Collapse Scenarios options.","Doc strings tagged with a <code>\\"\\"\\"json</code>, <code>\\"\\"\\"xml</code>, or <code>\\"\\"\\"yaml</code> (also <code>\\"\\"\\"yml</code>) fence are now treated as JSON / XML / YAML by the editor, with the same language services as HTML \u2014 syntax highlighting, fold markers on multi-line structures, and language-specific completion proposals \u2014 and matching \\"Collapse JSON / XML / YAML doc strings\\" toggles under Settings &gt; Tools &gt; SpecBinder &gt; Code folding that fold their bodies on file open.","New \\"Auto-open HTML previews\\" option under Settings &gt; Tools &gt; SpecBinder &gt; Doc string previews: when enabled, all <code>\\"\\"\\"html</code> doc strings are automatically rendered inline when a feature file is opened, without needing to click each gutter icon individually.","The HTML doc-string preview gutter icon now reflects the preview state at a glance: a neutral grey eye while the preview is hidden and a green eye while the preview is visible, so it is easy to see which <code>\\"\\"\\"html</code> doc strings are currently rendered.","When a failed step\'s execution report carries both <code>expected</code> and <code>actual</code> values, the inline error panel now shows a \\"Show diff\\" link beneath the Expected and Actual blocks. Clicking the link opens IntelliJ\'s standalone diff viewer on the two values, with line-level highlighting, so the comparison can be reviewed without leaving the editor.","The inline error panel now shows a \\"Visual diff\\" link beneath the Expected and Actual blocks when the failing step\'s parameter is an HTML-tagged doc string (<code>\\"\\"\\"html</code>). Clicking it opens a side-by-side rendered HTML diff viewer in a separate frame: each side renders inside a sandboxed iframe (CSS-isolated), and a parallel DOM walk outlines differing elements with dashed coloured boxes \u2014 green for modified, red for elements only in Actual, and orange for elements only in Expected. Indicators are off by default; an \\"Indicators\\" toggle button in the toolbar reveals them. The toolbar adapts to the IDE theme (white in light themes, dark in dark themes), shows the colour-key legend on the right, and on the left provides up / down arrow buttons that step through differing elements in the Actual pane and ring the current element with a non-displacing blue halo (a <code>box-shadow</code> ring so layout is unaffected); when the Actual element has a positional counterpart in the Expected pane that pane\'s matching element is also ringed so the user can compare both sides at a glance plus a \\"Hide indicators\\" / \\"Show indicators\\" toggle that hides or restores all of the highlight boxes in both panes. The viewer supports zoom via keyboard (Ctrl/Cmd + =, Ctrl/Cmd + -, Ctrl/Cmd + 0 to reset) and Ctrl/Cmd + mouse wheel, scaling the rendered HTML in both panes between 50% and 300%.","The inline error panel now shows an \\"Approve\\" link next to \\"Show diff\\" when a failed step\'s execution report carries both <code>expected</code> and <code>actual</code> values. Clicking it writes the runtime Actual value directly into the failing step\'s matching parameter (<code>\\"...\\"</code> literal or <code>\\"\\"\\"...\\"\\"\\"</code> doc string) \u2014 the same edit \\"Apply Expected to Step\\" performs in the diff viewer, but in one click and without opening the diff window. If the rewrite cannot be applied (no matching parameter, ambiguous match, or shape mismatch), the matcher\'s reason is shown as an editor hint so the failure is visible.","After a successful \\"Approve\\" or \\"Apply Expected to Step\\" rewrite, the editor caret is now placed at the end of the replaced value: just before the closing <code>\\"</code> for a simple parameter, or at the end of the last content line of a doc-string parameter. Editor focus is restored to the spec file so the user can keep typing.","When the failing step\'s parameter is an HTML-tagged doc string (<code>\\"\\"\\"html</code>), the inline error panel now renders a live HTML preview directly below the Expected and Actual values. Each preview is hosted in its own sandboxed iframe with full DOM and CSS isolation so the two snapshots cannot interfere with each other or with the panel\'s own styling, and the iframe auto-resizes to fit its content.","New \\"Inline error panel \u2014 Message / Expected / Actual folding\\" section under Settings &gt; Tools &gt; SpecBinder lets you choose the *initial* state of multi-line Message, Expected, and Actual values in the inline error panel: Never (start expanded), Always (start collapsed to the first line with an ellipsis), or When longer than N lines (default 10) \u2014 the new default, so short multi-line values stop starting collapsed. The fold/unfold affordance is always available on multi-line values regardless of this setting; the setting only controls whether they start expanded or collapsed. Single-line values (including values with only a trailing newline) always render in full as non-foldable.","The standalone diff viewer opened from the error panel detects the failing step\'s doc-string fence (<code>\\"\\"\\"html</code>, <code>\\"\\"\\"json</code>, <code>\\"\\"\\"xml</code>, <code>\\"\\"\\"yaml</code>, \u2026) and applies language-aware syntax highlighting, matched-bracket highlighting, and folding to both panes; doc strings without a fence and simple <code>\\"...\\"</code> parameters continue to render as plain text.","The standalone diff viewer opened from the error panel makes the Expected side editable while keeping Actual as a read-only runtime snapshot, so the standard gutter chunk-arrows are usable for copying differing pieces from Actual into Expected and the Expected pane can also be hand-edited. Two actions are contributed to the diff toolbar: \\"Copy Actual to Expected\\" overwrites the Expected pane with the runtime Actual value (equivalent to pressing every chunk-arrow at once) without touching the spec file or closing the viewer, and \\"Apply Expected to Step\\" writes the current Expected pane contents into the failing step\'s matching parameter (<code>\\"...\\"</code> literal or <code>\\"\\"\\"...\\"\\"\\"</code> doc string) and closes the viewer. The apply-to-step action is enabled only when exactly one parameter on the step has a literal value equal to the original expected and the candidate replacement legally fits the parameter shape; otherwise it is disabled with a tooltip explaining why.","New \\"Make Rules foldable\\" option under Settings &gt; Tools &gt; SpecBinder &gt; Code folding: when enabled, multi-line Rule blocks show a fold marker in the editor gutter and can be collapsed; when disabled (the default) Rule blocks stay non-foldable so the gutter stays uncluttered. The \\"Collapse Rules\\" option is only available while Rules are foldable."]},{"type":"Changed","items":["The embedded HTML previews under Expected and Actual in the inline error panel now align flush with the value text above them \u2014 the preview\'s left edge sits at exactly the same x-coordinate as the value column instead of starting one chevron-column further left","The Approve / Show diff / Visual diff action buttons row in the inline error panel now starts flush with the value column too, so its left edge lines up with the value text rows above and the embedded HTML previews below \u2014 previously the row started one chevron-column further left","HTML doc-string previews no longer show the IDE\'s CSS color-preview gutter swatches beside <code>style=\\"...\\"</code> attributes that contain color literals, so the gutter stays clean and the preview\'s own gutter icon is the only marker on those lines","Long Message, Expected, and Actual values in the inline error panel now collapse to their first line by default, with an ellipsis (<code>\u2026</code>) indicator at the end of the line; clicking the value expands it to reveal the remaining lines, and clicking it again collapses it back. Single-line values continue to render plainly without any disclosure affordance, so terse failures stay terse","Folding typed doc strings (<code>\\"\\"\\"html</code>, <code>\\"\\"\\"json</code>, <code>\\"\\"\\"xml</code>, <code>\\"\\"\\"yaml</code>) now collapses the entire block into a single line showing the opening fence with a placeholder (e.g. <code>\\"\\"\\"html...</code>), with the fold/unfold gutter marker on the opening fence line \u2014 matching the standard IDE folding convention for other languages","Switched to calendar-year-based versioning (major version now reflects the release year)","Step auto-completion now always shows the suggestion popup, even when only one step matches, so the suggestion (including any parameter placeholders) can be reviewed before inserting","Text wrapped in <code>&lt;...&gt;</code> inside a doc string that does not match any Examples table column header is now rendered as plain doc-string text instead of being highlighted as an unresolved outline parameter"]},{"type":"Fixed","items":["Failed-step error gutter icons are now always hidden when the recorded outcome is stale, regardless of the \\"Show stale icons\\" setting; the setting continues to govern scenario, rule, and feature level icons","Indentation detection no longer overrides configured code style settings for <code>.specb</code> files, which could cause incorrect formatting (e.g., table rows moving to column 0)","Picking a step suggestion from the autocompletion popup now replaces the already typed step text instead of appending the chosen pattern to it","Gutter icons for Scenarios inside a Rule now correctly reflect the test execution state when the generated test class is abstract and a separate concrete subclass runs the tests","Feature and Rule gutter icons now stay red whenever any child scenario is recorded as failed, even after re-running only the passing siblings (which previously caused the container icon to incorrectly turn green)","The inline error panel and the inline HTML doc-string preview no longer show a spinner indefinitely when the embedded browser fails to render: a 5-second watchdog reveals the content at a fallback height, and an in-panel error message is surfaced if the browser reports a load failure"]},{"type":"Removed","items":["The \\"Duration\\" field has been dropped from the inline error panel; the error message, expected/actual values, and stack trace remain the on-demand failure summary, with the step\'s run time available in the standard run tool window if needed"]}]},{"version":"0.9.0","date":"2026-04-26","unreleased":false,"sections":[{"type":"Added","items":["Re-running individual scenarios from the test results tree now correctly redirects to the concrete subclass when the test method is declared in an abstract generated class","Gutter run icons in feature files now also redirect to the concrete subclass when the generated test class is abstract","Run configuration names from feature files now show the Scenario/Rule/Feature title instead of the Java method name","Support for escaped double quotes (<code>\\\\\\"</code>) inside quoted step parameters, matching SpecBinder\'s escaping rules","Rename refactoring for annotation patterns with regex capture groups preserves captured values in step text"]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1032905"},{"version":"0.8.0","date":"2026-04-04","unreleased":false,"sections":[{"type":"Added","items":["Rename refactoring for Scenario Outline parameters \u2014 invoke Rename on a <code>&lt;parameter&gt;</code> placeholder or Examples table header to rename all occurrences within the same Scenario Outline"]},{"type":"Fixed","items":["Fix rename of step annotation patterns not propagating changes to matching step text in feature files"]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1008730"},{"version":"0.7.0","date":"2026-04-04","unreleased":false,"sections":[{"type":"Changed","items":["Updated Marketplace plugin description with comprehensive feature list"]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1008504"},{"version":"0.6.0","date":"2026-04-04","unreleased":false,"sections":[{"type":"Fixed","items":["Remove usage of deprecated IntelliJ Platform API"]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1008480"},{"version":"0.5.0","date":"2026-04-04","unreleased":false,"sections":[{"type":"Added","items":["Added autocompletion for Gherkin keywords","testing automated release process"]}],"url":"https://plugins.jetbrains.com/plugin/30540/versions/stable/1008477"},{"version":"0.2.1","date":"2026-03-11","unreleased":false,"sections":[{"type":"Fixed","items":["Fix crash when installed alongside the Cucumber/Gherkin plugin caused by conflicting TextAttributesKey names"]}]},{"version":"0.2.0","date":"2026-03-11","unreleased":false,"sections":[{"type":"Added","items":["Ctrl/Cmd+Click navigation from feature file elements to SpecBinder-generated test classes","Ctrl/Cmd+Click navigation from Java test methods back to Gherkin steps","Auto-complete step text from Cucumber annotations and method names","Find Usages on Java step methods shows matching steps in feature files","Shift+F6 rename on Cucumber annotation strings updates step patterns across feature files","Gutter run icons on Feature/Rule/Scenario keywords with JUnit run configuration support","Structure tool window showing Feature/Rule/Scenario/Step outline","Code folding for Feature, Rule, Scenario, Background, Examples blocks","Code formatting with 2-space Gherkin indentation (Cmd+Alt+L)"]}]},{"version":"0.1.0","date":"2026-03-11","unreleased":false,"sections":[{"type":"Added","items":["Gherkin syntax highlighting with per-keyword color customization","Color settings page (Settings &gt; Editor &gt; Color Scheme &gt; Gherkin)"]}]}]}]}'),N="main_XyMz",j="header_Hvnt",J="pageTitle_oyiU",M="subtitle_CEca",I="intro_ezcO",O="tagline_M4pt",G="marketplaceCard_MrIs",B="panel_uVj8",D="version_SrhU",L="versionTitle_aUim",U="versionLink_IM23",F="date_XUih",H="badge_zXbG",P="badgeLatest_nrCy",_="badgeUnreleased_UM9t",q="section_DD9W",V="sectionTitle_SR9_",z="items_bc2P",W="empty_UsM6",$={Added:"added_sPvT",Changed:"changed_JU1E",Fixed:"fixed_zHz6",Removed:"removed_to2i"},X=["January","February","March","April","May","June","July","August","September","October","November","December"];function Y({version:e,isLatest:t,urlTemplate:n}){const o=function(e){const t=/^(\d{4})-(\d{2})-(\d{2})$/.exec(e||"");return t?`${X[Number(t[2])-1]} ${Number(t[3])}, ${t[1]}`:null}(e.date),a=e.url||(n?n.replace("{version}",encodeURIComponent(e.version)):null);return(0,k.jsxs)("section",{className:D,children:[(0,k.jsxs)("h2",{id:`v-${e.version}`,className:L,children:[a?(0,k.jsx)("a",{className:U,href:a,target:"_blank",rel:"noopener noreferrer",children:e.version}):e.version,o&&(0,k.jsx)("time",{className:F,dateTime:e.date,children:o}),e.unreleased&&(0,k.jsx)("span",{className:(0,r.A)(H,_),children:"Unreleased"}),t&&(0,k.jsx)("span",{className:(0,r.A)(H,P),children:"Latest"})]}),e.sections.map(e=>(0,k.jsxs)("div",{className:(0,r.A)(q,$[e.type]),children:[(0,k.jsx)("h3",{className:V,children:e.type}),(0,k.jsx)("ul",{className:z,children:e.items.map((e,t)=>(0,k.jsx)("li",{dangerouslySetInnerHTML:{__html:e}},t))})]},e.type))]})}function K({product:e}){const t=(e.versions||[]).filter(e=>!e.unreleased);if(0===t.length)return(0,k.jsx)("p",{className:W,children:"No release notes available yet."});const n=t[0];return(0,k.jsxs)("div",{className:B,children:[(0,k.jsxs)("div",{className:I,children:[e.tagline&&(0,k.jsx)("p",{className:O,children:e.tagline}),e.embedCardUrl&&(0,k.jsx)("iframe",{className:G,src:e.embedCardUrl,width:"384",height:"260",loading:"lazy",title:`${e.label} on JetBrains Marketplace`})]}),t.map(t=>(0,k.jsx)(Y,{version:t,isLatest:t.version===n.version,urlTemplate:e.versionUrlTemplate},t.version))]})}function Q(){const e=R.Z||[];return(0,k.jsx)(o.A,{title:"Release Notes",description:"Version-by-version release notes for the SpecBinder framework and IntelliJ IDEA plugin.",children:(0,k.jsxs)("main",{className:(0,r.A)("container",N),children:[(0,k.jsxs)("header",{className:j,children:[(0,k.jsx)("h1",{className:J,children:"Release Notes"}),(0,k.jsx)("p",{className:M,children:"What changed in each release of the SpecBinder framework and the IntelliJ IDEA plugin."})]}),(0,k.jsx)(E,{groupId:"product",queryString:"product",children:e.map(e=>(0,k.jsx)(A,{value:e.key,label:e.label,children:(0,k.jsx)(K,{product:e})},e.key))})]})})}}}]);